import 'package:flutter_test/flutter_test.dart';
import 'package:ods_flutter/ods_flutter.dart';

void main() {
  test('check the theme existence when loading ods_flutter lib', () {
    expect(totoThemeData, isNot(null));
  });
}
